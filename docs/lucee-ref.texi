\input texinfo @c -*-texinfo-*-
@c This will be for the printing version of the manual
@c @input config-local.texi
@c %**start of header
@setfilename lucee-ref.info
@settitle Lucee: Computational Physics Framework
@finalout
@ifset publish
@set frontcontents
@setchapternewpage odd
@end ifset
@c %**end of header

@c How to use the math macros
@c ==========================
@c 
@c For simple expressions, simply use the @math{} command, e.g.
@c
@c     @math{\exp(x)/(1+x^2)}
@c 
@c but if the expression includes characters that need to be 'escaped'
@c in texinfo, like '{' or '}', or needs different output for TeX and info,
@c then use the following form,
@c
@c     blah blah blah @c{$y^{1+b} \le \pi$}
@c     @math{y^@{1+b@} <= \pi}
@c
@c The first part using @c{} must appear at the end of a line (it reads
@c up to the line end -- as far as texinfo is concerned it's actually
@c a 'comment').  The comment command @c has been modified to capture
@c a TeX expression which is output by the next @math.
@c
@c For ordinary comments use the @comment command.

@c @tex
@c % Mathematical macros taken from the GNU Calc Manual
@c % ==================================================
@c %
@c % Some special kludges to make TeX formatting prettier.
@c % Because makeinfo.c exists, we can't just define new commands.
@c % So instead, we take over little-used existing commands.
@c %
@c % Redefine @cite{text} to act like $text$ in regular TeX.
@c % Info will typeset this same as @samp{text}.
@c \gdef\goodtex{\tex \let\rm\goodrm \let\t\ttfont \turnoffactive}
@c \gdef\goodrm{\fam0\tenrm}
@c \gdef\math{\goodtex$\mathxxx}
@c \gdef\mathxxx#1{#1$\Etex}
@c \global\let\oldxrefX=\xrefX
@c \gdef\xrefX[#1]{\begingroup\let\math=\dfn\oldxrefX[#1]\endgroup}
@c %
@c % Redefine @i{text} to be equivalent to @cite{text}, i.e., to use math mode.
@c % This looks the same in TeX but omits the surrounding ` ' in Info.
@c %\global\let\i=\cite
@c %\global\let\math=\cite
@c %
@c % Redefine @c{tex-stuff} \n @whatever{info-stuff}.
@c \gdef\c{\futurelet\next\mycxxx}
@c \gdef\mycxxx{%
@c   \ifx\next\bgroup \goodtex\let\next\mycxxy
@c   \else\ifx\next\mindex \let\next\relax
@c   \else\ifx\next\kindex \let\next\relax
@c   \else\ifx\next\starindex \let\next\relax \else \let\next\comment
@c   \fi\fi\fi\fi \next
@c }
@c \gdef\mycxxy#1#2{#1\Etex\mycxxz}
@c \gdef\mycxxz#1{}
@c %
@c % Define \Hat to take over from \hat as an accent
@c \gdef\Hat{\mathaccent "705E}
@c %
@c \gdef\beforedisplay{\vskip-10pt}
@c \gdef\afterdisplay{\vskip-5pt}
@c \gdef\beforedisplayh{\vskip-25pt}
@c \gdef\afterdisplayh{\vskip-10pt}
@c %
@c \gdef\arcsec{\hbox{\rm arcsec}}
@c \gdef\arccsc{\hbox{\rm arccsc}}
@c \gdef\arccot{\hbox{\rm arccot}}
@c \gdef\sech{\hbox{\rm sech}}
@c \gdef\csch{\hbox{\rm csch}}
@c \gdef\coth{\hbox{\rm coth}}
@c \gdef\arcsinh{\hbox{\rm arcsinh}}
@c \gdef\arccosh{\hbox{\rm arccosh}}
@c \gdef\arctanh{\hbox{\rm arctanh}}
@c \gdef\arcsech{\hbox{\rm arcsech}}
@c \gdef\arccsch{\hbox{\rm arccsch}}
@c \gdef\arccoth{\hbox{\rm arccoth}}
@c %
@c \gdef\Re{\hbox{\rm Re}}
@c \gdef\Im{\hbox{\rm Im}}
@c \gdef\Sin{\hbox{\rm Sin}}
@c \gdef\Cos{\hbox{\rm Cos}}
@c \gdef\Log{\hbox{\rm Log}}
@c %
@c \gdef\erf{\hbox{\rm erf}}
@c \gdef\erfc{\hbox{\rm erfc}}
@c \gdef\sinc{\hbox{\rm sinc}}
@c @end tex

@titlepage
@title Lucee: Computational Physics Framework
@subtitle Reference Manual

@author Ammar H. Hakim
Boulder, Colorado
@sp 1

@end titlepage

@comment @ifset frontcontents
@contents
@comment @end ifset

@chapter Basic Classes
@include basic.texi

@bye
